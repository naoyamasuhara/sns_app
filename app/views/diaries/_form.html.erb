<%= form_for @diary do |f| %>
  <%= f.label :タイトル %><br>
  <%= f.text_field :title, class: 'form-control diary_form' %>
  <%= f.label :コメント %><br>
  <%= f.text_area :content, class: 'form-control diary_form' %>
  <div align="right"><%= f.submit "送信する", class: "btn btn-success" %></div>
<% end %>